# Node
node_modules/
dist/
*.tsbuildinfo

# Gradle / Java
.gradle/
build/
*.class

# OS
.DS_Store

# Local state (never commit a real account's exported data)
.wise-old-ai/

# Jagex session tokens — never commit (see docs/serving-jagex-credentials.md)
*jx-credentials*
